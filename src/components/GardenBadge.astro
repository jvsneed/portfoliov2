<a href="/garden" class="garden-badge" aria-label="Digital Garden">
    <div class="circular-text">
        <svg viewBox="0 0 100 100">
            <defs>
                <path id="circle-path" d="M50,50m-30,0a30,30 0 1,1 60,0a30,30 0 1,1-60,0"/>
            </defs>
            <text>
                <textPath href="#circle-path" startOffset="0%">
                    digital  • garden •
                </textPath>
            </text>
        </svg>
    </div>
    <div class="badge-icon">🪴</div>
</a>

<style>
    .garden-badge {
        position: fixed;
        right: 2rem;
        bottom: 2rem;
        width: 3rem;
        height: 3rem;
        background: var(--color-surface);
        border: 1px solid var(--color-border);
        border-radius: 50%;
        display: var(--garden-badge-display, flex);
        align-items: center;
        justify-content: center;
        text-decoration: none;
        box-shadow: var(--shadow-soft);
        z-index: 99;
        transition: all 0.3s var(--ease-out-soft);
    }

    .circular-text {
        position: absolute;
        width: 200%;
        height: 200%;
        left: -50%;
        top: -50%;
        animation: spin 20s linear infinite;
        pointer-events: none;
        background-color:rgba(255, 255, 255, 0.7);
        border-radius: 50%;
    }

    .circular-text svg {
        width: 100%;
        height: 100%;
    }

    .circular-text text {
        font-size: .8rem;
        font-family: var(--font-mono);
        fill: var(--color-muted);
        text-transform: uppercase;
        letter-spacing: 2px;
        font-weight: 500;
    }

    .badge-icon {
        font-size: 1.3rem;
        background: var(--color-surface);
        width: 2.5rem;
        height: 2.5rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 2;
    }

    .garden-badge:hover {
        border: 2px solid var(--color-accent);
        transform: scale(1.1);
    }

    .garden-badge:hover .circular-text text {
        fill: var(--color-accent);
    }

    @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }

    @media (max-width: 768px) {
        .garden-badge {
            right: 2em;
            bottom: 2rem;
            width: 3rem;
            height: 3rem;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .circular-text {
            animation: none;
        }
    }
</style>
