<a href="/garden" class="garden-badge" aria-label="Digital Garden">
    <div class="circular-text">
        <svg viewBox="0 0 100 100">
            <defs>
                <path id="circle-path" d="M50,50m-30,0a30,30 0 1,1 60,0a30,30 0 1,1-60,0"/>
            </defs>
            <text>
                <textPath href="#circle-path" startOffset="0%">
                    digital  • garden •
                </textPath>
            </text>
        </svg>
    </div>
    <div class="badge-icon">🪴</div>
</a>

<style>
    .garden-badge {
        position: fixed;
        right: 2rem;
        bottom: 2rem;
        width: 3rem;  /* Increased overall size */
        height: 3rem; /* Increased overall size */
        background: var(--color-surface);
        border: 1px solid var(--color-border);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        box-shadow: var(--shadow-soft);
        z-index: 99;
        transition: all 0.3s var(--ease-out-soft);
    }

    .circular-text {
        position: absolute;
        width: 200%;    /* Made larger for bigger text ring */
        height: 200%;   /* Made larger for bigger text ring */
        left: -50%;     /* Adjusted from -40% */
        top: -50%;      /* Adjusted from -40% */
        animation: spin 20s linear infinite;
        pointer-events: none;
        background-color:rgba(255, 255, 255, 0.4);
        border-radius: 50%;
    }

    .circular-text svg {
        width: 100%;
        height: 100%;
    }

    .circular-text text {
        font-size: .8rem;          /* Increased from 9px */
        font-family: var(--font-mono);
        fill: var(--color-muted);
        text-transform: uppercase;
        letter-spacing: 2px;    /* Adjusted from 1.5px */
        font-weight: 500;        /* Added for better visibility */
    }

    .badge-icon {
        font-size: 1.3rem;       /* Slightly reduced from 1.4rem */
        background: var(--color-surface);
        width: 2.5rem;           /* Added fixed width for inner circle */
        height: 2.5rem;          /* Added fixed height for inner circle */
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 2;
    }

    .garden-badge:hover {
        border: 2px solid var(--color-accent);
        transform: scale(1.1);
    }

    .garden-badge:hover .circular-text text {
        fill: var(--color-accent);
    }

    @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }

    @media (max-width: 768px) {
        .garden-badge {
            right: 1rem;
            bottom: 1rem;
            width: 4.5rem;         /* Adjusted mobile size */
            height: 4.5rem;        /* Adjusted mobile size */
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .circular-text {
            animation: none;
        }
    }
</style>
